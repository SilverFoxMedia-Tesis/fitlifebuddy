import 'package:fitlifebuddy/core/theme/spacing/space.dart';
import 'package:flutter/material.dart';

class AppPadding {
  static const paddingXs = EdgeInsets.all(Space.spaceXs);
  static const paddingSm = EdgeInsets.all(Space.spaceSm);
  static const paddingMd = EdgeInsets.all(Space.spaceMd);
  static const paddingLg = EdgeInsets.all(Space.spaceLg);
  static const paddingXl = EdgeInsets.all(Space.spaceXl);
  static const paddingXxl = EdgeInsets.all(Space.spaceXxl);
  static const paddingXxxl = EdgeInsets.all(Space.spaceXxxl);

  static const paddingHorizontalXs = EdgeInsets.symmetric(horizontal: Space.spaceXs);
  static const paddingHorizontalSm = EdgeInsets.symmetric(horizontal: Space.spaceSm);
  static const paddingHorizontalMd = EdgeInsets.symmetric(horizontal: Space.spaceMd);
  static const paddingHorizontalLg = EdgeInsets.symmetric(horizontal: Space.spaceLg);
  static const paddingHorizontalXl = EdgeInsets.symmetric(horizontal: Space.spaceXl);
  static const paddingHorizontalXxl = EdgeInsets.symmetric(horizontal: Space.spaceXxl);
  static const paddingHorizontalXxxl = EdgeInsets.symmetric(horizontal: Space.spaceXxxl);
  
  static const paddingVerticalXs = EdgeInsets.symmetric(vertical: Space.spaceXs);
  static const paddingVerticalSm = EdgeInsets.symmetric(vertical: Space.spaceSm);
  static const paddingVerticalMd = EdgeInsets.symmetric(vertical: Space.spaceMd);
  static const paddingVerticalLg = EdgeInsets.symmetric(vertical: Space.spaceLg);
  static const paddingVerticalXl = EdgeInsets.symmetric(vertical: Space.spaceXl);
  static const paddingVerticalXxl = EdgeInsets.symmetric(vertical: Space.spaceXxl);
  static const paddingVerticalXxxl = EdgeInsets.symmetric(vertical: Space.spaceXxxl);

  static const paddingVerticalXxxlHorizontalLg = EdgeInsets.symmetric(vertical: Space.spaceXxxl, horizontal: Space.spaceLg);
  static const paddingHorizontal100 = EdgeInsets.symmetric(horizontal: 100);
  static const paddingPage = EdgeInsets.symmetric(vertical: 32, horizontal: 48);
}